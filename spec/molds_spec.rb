# frozen_string_literal: true

RSpec.describe Molds do
  it 'has a version number' do
    expect(Molds::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to be_falsy
  end
end
